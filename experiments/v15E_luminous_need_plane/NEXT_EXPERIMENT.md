# Next experiment — v15FG

Correct the screen semantics: transmit only the scalar intermediate/need while the same AI context remains shared. Separate that mechanism from optional same-AI self-discussion. Keep hardware revision requests tied only to persistent physical-health evidence.