# v15E Idea Ledger

Retained: spatial/relational memory, guided volatile gap inference, signed dendrite charge, natural decay, HZO slow polarity, four active + two repair branches, zero MOS ordinary semantic cells, need-based replay, trusted durable teaching, hollow shared infrastructure, and maker-facing hardware revision requests only for persistent physical faults.

Added: package-level Luminous Need Plane and direct light-to-shared-power-gate activation.

Rejected: FITA, SEEP, single-pixel optical destination code, and hardware-change requests caused merely by unknown knowledge.