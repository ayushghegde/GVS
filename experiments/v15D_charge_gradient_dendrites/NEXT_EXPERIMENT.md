# Next experiment — v15D1 Charge Residue / Analog HZO Coupon

Close only the new physical unknown introduced by v15D:
1. calibrate how much signed charge residue remains after one guided-gap event;
2. measure its decay over ~0.1–10 µs;
3. apply repeated positive and negative event trains and measure partial 4-nm HZO polarization;
4. verify that one-off events produce no durable change;
5. verify that repeated opposite-sign events neutralize/reverse an old route;
6. measure guided-gap choice margin versus HZO polarization;
7. only after the device curve is calibrated, rerun full region PVT/mismatch and close the remaining shared-MOS launcher/interface count.

Follow-up correction: v15E replaces the fixed 15–25e target with event-derived Q=C·V and finds tens-of-electrons capture plausible in ngspice/Monte-Carlo. A physical compound coupon is still required; reject DCG if measured capture/leakage cannot reproduce that envelope. Do not add branch MOS to force a pass.
